@page
@model Sahty.Web.Pages.Account.LogoutModel
@{
    ViewData["Title"] = "Déconnexion";
}

<div class="auth-hero">
    <div class="auth-card">
        <div>
            <div class="pill mb-2">Déconnexion</div>
            <h1 class="h5 mb-2">Redirection...</h1>
            <p class="text-muted">Si tu vois ce message, la redirection va s'effectuer vers la page de connexion.</p>
        </div>
    </div>
</div>
